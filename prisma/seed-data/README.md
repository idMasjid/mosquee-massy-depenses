# Fichiers CSV d'import

Placez ici les exports CSV du Google Sheet historique :

- `detail-depenses.csv` — export de l'onglet "Détail dépenses"
- `projets.csv` — export de l'onglet "Projets" (ou "Recap")

Puis lancez `npm run db:seed`.

Ces fichiers sont ignorés par Git (voir `.gitignore`).
